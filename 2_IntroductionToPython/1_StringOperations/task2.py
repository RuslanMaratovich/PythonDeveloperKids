# str.upper() — возвращает строку с заглавными буквами.
#
# str.lower() — возвращает строку с малыми буквами.
#
# len() — получение длины строки (не только к строкам).


line_1 = "Python"
line_2 = "Java"
line_3 = "Go"

all_length = len(line_1) + len(line_2) + len(line_3)
print(all_length // 3)
